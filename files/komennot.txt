nginx config: /etc/nginx/sites-available/default
start runner: sudo ./svc.sh start
frontend folder: /var/www/FileServer/_work/FileServer/FileServer/frontend/fileserver/build

runner permission: sudo chmod -R 777 /var/www/FileServer

ssh -i FileServer root@165.22.80.179

postgres access: sudo su - postgres
psql

folders: CREATE TABLE folders (id SERIAL PRIMARY KEY, name VARCHAR(100) NOT NULL);

files: CREATE TABLE files(id SERIAL PRIMARY KEY,  file VARCHAR(100), folder VARCHAR(100), date DATE NOT NULL DEFAULT CURRENT_DATE);